void main(List<String> args) {
  // type promotion
  // there is a spepcial + operator for double that is used for adding
  // deciamals and num in double

  const int1 = 1;
  const double1 = 1.0;
  const double result = int1 + double1;
  print(result);
}
